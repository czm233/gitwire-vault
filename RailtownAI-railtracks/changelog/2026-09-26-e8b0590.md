# Changelog — 2026-09-26（`1c3ab8c` → `e8b0590`）

本次同步为纯依赖升级：2 个提交，仅触碰 `packages/railtracks/pyproject.toml` 与 `uv.lock`，无源代码/API 变更。

## 新增

- 无

## 修复

- litellm 版本约束升级为 `>=1.101.0,<=1.102.1`（原 `>=1.84.0,<=1.89.0`），目的是支持 gpt-6 的 `reasoning_effort` 参数（c2acf29，#1596/#1600，pyproject.toml）。uv.lock 同步锁定到 litellm 1.102.1，该版本改为发布平台 wheel（cp310-abi3，多平台二进制），新增 `boto3`、`pydantic-settings` 依赖（uv.lock）。

## 重构

- 无

## 破坏性变更

- 无（对 railtracks 自身 API 而言无变更；但依赖侧注意：litellm 约束下限从 1.84.0 大幅抬升，且 anyio 从 4.10.0 升至 4.15.1、pydantic-settings 从 2.10.1 升至 2.15.0，依赖上游变化需在下游集成时复核兼容性，e8b0590 + c2acf29，uv.lock）

## 情报备注

- litellm 拆分为平台 wheel 且体积从约 15MB 增至约 27MB、引入 boto3，说明上游正在扩大云服务集成面（推断，信源 uv.lock diff，具体动机未核实）。
- anyio 4.15.1 移除 sniffio 直接依赖、typing-extensions marker 扩至 python<3.15（uv.lock diff）。