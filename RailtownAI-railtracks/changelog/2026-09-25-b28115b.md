# Changelog — 2026-09-25（b28115b）

模式：init（首次全量建档）。本次为基线快照，无前一游标可比对，故不逐条归因 commit。

## 新增
- 全仓库基线建档，覆盖 `b28115b`（2026-09-25）快照。要点：
  - 纯 Python agent 框架 `railtracks`（uv workspace，MIT）。
  - 五部件 harness 模型：Loop / Tool surface / Context / Controls / Record。
  - 双层中间件（node 级 / model 级）、verifier 人审体系、预置 guardrails 与工具集（ToDo、KeyValueMemory、WebSearch）。
  - MCP 双向集成（connect_mcp / create_mcp_server）。
  - retrieval 子系统（Pgvector / Chroma / ChromaCloud / InMemory 后端）与 evaluations 子系统。
  - CLI（`railtracks viz`、`_skillkit` 多 provider 技能安装）。
  - 官方 harness 示例（minimal / coding）与 HIL 示例套件（含真实 webhook 审批 demo）。
  - 信源：README.md、examples/、docs/、packages/railtracks/src/ 快照。

## 修复
- 无（首次建档，无 diff 基线）。

## 重构
- 无（首次建档，无 diff 基线）。

## 破坏性变更
- 无（首次建档，无 diff 基线）。
- 备注：docs/documentation/upgrading/1_5_0.md 与 pyproject 的 FutureWarning 过滤规则（`'error:.*in railtracks 1\.5\.0:FutureWarning'`）表明 1.5.0 曾有弃用升级，1.5.* 系列期间相关警告按错误处理；具体破坏性条目见该升级文档，本次未逐条核实。