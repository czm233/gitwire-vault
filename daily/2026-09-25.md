# 2026-09-25 晨报

> railtracks 单日四个 commit 密集推进观测层与 LLM 推理透出（verifier 事件、reasoning blocks 全链路），同时 issue 池出现多起 schema/类型静默退化类 bug，配套修复节奏明显加快。

## 最值得关注
- **verifier 从静默到可观测（railtracks，1c3ab8c / #1425）**：新增 6 种 verifier 事件与 `VerifierDecision` 结构，且 CLI viz 把 verifier 纳入 Middleware 分类与会话 Blocked 判定；事件命名与 decision 结构全面镜像 guardrail 体系，显示上游正把「人工审核」统一进「策略执行」抽象。对依赖事件流做监控/审计的下游是新事件类型，按 event_type 白名单消费的分析器需注意。
- **LLM reasoning/thinking 全链路透出（railtracks，ee5d84d / #1431，已关闭）**：`AssistantMessage` 新增 `reasoning_content`/`thinking_blocks`，覆盖流式、结构化输出、工具调用循环，且处理 Anthropic 签名块回传语义。#1217（thinking 模型全链路支持）同日关闭，说明这条架构线已实质性落地。
- **`Middleware` 泛型加参引发的连锁修复（railtracks，2f1626e + 75c3b17 / #1538、#1590 均已关闭）**：为修类型坍缩给 `Middleware` 加第三泛型参数 `_Constraint`，但随即在 Python 3.11+ 引入 `BaseGuardrail` 导入崩溃（#1590，当日即由 PR #1591 修复）。改动波及面广，值得观察是否还有跨模块泄漏的后续问题（#1593 已报告 3.13+ 相关 bug）。

## 各项目动态

### RailtownAI/railtracks
- `b28115b`：init 首次全量建档，基线快照无 diff；备注 1.5.0 曾有 FutureWarning 升级（b28115b）。
- `1c3ab8c`（#1425）：verifier 事件透出 + viz 分类；`VerifierRejectedError` 加入 Blocked 判定、`middleware.failure` 分支修复 `raised_here` 误判异常来源（1c3ab8c）。
- `2f1626e`（#1538）：`function_node` 中间件组合类型坍缩修复，`Middleware` 新增第三泛型参数 `_Constraint`（带默认值保持兼容），配套 161 行类型回归脚本与多中间件组合测试（2f1626e）。
- `75c3b17`（#1591）：修复 3.11+ 下 `BaseGuardrail` 导入失败；另 #1583 大改 agent-facing 文档（AGENTS.md、skills 改为面向 coding agent 的机读指引），rag-pipeline 技能新增 embedder 选择规则（75c3b17 / 4726cf2）。
- `ee5d84d`（#1431/#1563）：reasoning/thinking blocks 全链路透出，`copy_metadata_from` 修复工具调用循环丢元数据；流式签名块重组依赖 litellm `_assemble_thinking_blocks`，细节未核实（ee5d84d）。

### browser-use/jev-ultrafast
- init 首次建档：v0.1.0 动态索引化动作空间浏览器 Agent，含原子 DOM 快照器与性能数据（中位数 9.450s→7.092s，CDP 1,092→101；README 数据，docs/performance.md 未在快照中，未核实）（1231850）。

## Issue 风向
- **schema/类型静默退化成为集中抱怨点**：#1458（`from __future__ import annotations` 下 schema 退化）、#1562（`Tool.from_function` 未映射类型降级为 object，绕过 #1552 严格校验）、#1581（`Optional[X]` 被误判为非必填）、#1582（异构元组缺按位约束）——反映工具调用正确性校验正在收紧，社区在为严格化查漏。
- **可观测性/事件化方向持续加码**：#1239（观测重构为事件流）、#1446（context 事件，已被认领）、#1454（静态节点事件）、#1415（强制退出后发终止事件）、#1413（日志迁移事件驱动）——与当日 1c3ab8c 落地方向一致。
- **Retrieval 子系统密集报障**：#1167（HF 加载器所有行同 id 破坏 upsert）、#1185（用户 metadata 可覆盖保留键）、#1187（部分失败 re-ingest 删掉旧好版本）、#1184/#1150（批量写需求）、#1186（Chroma 双份存储）——数据损坏类 bug 值得优先跟踪。
- **人审/护栏与富媒体是下阶段大方向**：#1265（Naive HIL）、#1266（通用 Verifier）、#1306（ScopeGuard）、#1065（被拦截护栏重试）；#1347 富媒体 Epic、#1348/#1349（工具返回富内容、音视频附件）。
- 机会池进出：#1547、#1588、#1589、#1592、#1593、#1594、#1598、#1599 重回机会池；#1594（多 Python 版本 CI 矩阵）与 #1573（`railtracks add --force` 参数顺序 bug）是定位明确的低门槛任务。
- 已关闭：#1431（PR#1563）、#1217、#1538（PR#1541）、#1590（PR#1591）——修复响应当日完成，项目活跃度高。
- shibing624/agentica：仅 #28（社区增长建议，简单，纯文档），无风险。

## 明日追踪
- **#1593（Python 3.13+ 兼容）**：与 #1590 同源（PEP 696/类型系统边缘），且当日已发生一次泛型加参引发 3.11+ 导入崩溃，#1593 是否升级为阻塞值得盯。
- **#1596（litellm 固定 ≤1.89.0 拒绝 gpt-6-luna 的 reasoning_effort）**：ee5d84d 深度依赖 litellm 内部行为（`_assemble_thinking_blocks`），litellm 版本策略会直接影响 reasoning 功能稳定性。
- **#1187 / #1185（Retrieval 数据损坏类 bug）**：会删好版本、可写坏保留键，若上游开始拆批量写接口（#1150/#1184）需同步关注修复顺序。
- **#1239（观测重构为事件流）** 与 #1446（已被认领）的推进，将决定 verifier/context 等新事件是否面临二次迁移。
