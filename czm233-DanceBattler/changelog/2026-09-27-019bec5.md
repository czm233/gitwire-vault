# Changelog — 2026-09-27 (019bec5)

**同步类型**：init 首次全量建档（游标 `∅` → `019bec5`）。本次为基线快照，无逐 commit diff 可比对，故全部条目归入「新增（基线）」，commit 信源统一为本次同步头 `019bec5`。

## 新增

- 全量基线入库：后端 Koa 分层架构（routes/controllers/services/middleware/schedulers），信源 019bec5 仓库快照。
- 圣华 ShApi 客户端封装（歌曲/玩家/舞团/成绩/资源/装备），信源 backend/utils/ShapiClient.js（23KB）。
- 机台同步体系：定时任务 + 手动触发 + 去重 + 高德地理编码回填，信源 MachinePlaceScheduler.js、MachinePlaceService.js、scripts/backfillGeocode.js。
- 资源扫描工具链（scanResourceIds / viewScanResults / import-resources / check-data），信源 scripts/README.md。
- 部署运维链：webhook 自动部署 + 内存部署锁 + PM2 内存上限 + 4GB swap 脚本，信源 scripts/webhook-server.js、deploy.sh、scripts/add-swap.sh、ecosystem.*.config.js。
- 数据库唯一约束 `unique_place_name_location (place_name, province, city)`，信源 scripts/add-unique-constraint.sql、prisma/schema.prisma。
- 前端 React SPA：地图、排行榜、统计（8 视图）、资源列表、排队等页面，信源 frontend/src/。

## 修复

无（init 基线，无历史 commit 序列可比对）。

## 重构

无。

## 破坏性变更

无。

## 备注

- 机台去重问题曾导致 228 条重复数据，修复方案（去重脚本 + 唯一约束 + 同步逻辑改造）已包含在基线中，具体 commit 未核实（信源 scripts/README.md，署期 2026-02-02）。
- changelog 中出现 2026 年迁移目录（prisma/migrations/2026*）与 2026-09 同步日期，时间线自洽。