# Changelog 2026-09-24（init → 0b2db47）

首次全量建档（游标 `∅ → 0b2db47`）。以下条目基于快照现状归纳，无法逐条对应独立 commit，统一信源为快照 `0b2db47`。

## 新增

- 全量档案建立：用户/角色/权限 RBAC、管理后台、画师/单主双身份 App、委托排单与计时工作台（0b2db47）
- 邮箱验证码登录（console/smtp/resend 三通道，生产禁用 console）（0b2db47，backend/src/lib/email.ts）
- QQ OAuth 登录与首次身份引导 onboarding（0b2db47，backend/src/lib/qq-oauth.ts）
- 腾讯云 COS 图片直传链路：原图/预览图分离、短时签名 URL、站点/环境命名空间强制隔离（0b2db47，backend/src/lib/cos.ts）
- 表单邀请（自定义字段、幂等领取、图片字段）与公开排单分享/预览（0b2db47，backend/prisma/migrations/20260830120000、20260831100000）
- 本地开发快捷登录 dev-login（服务端 fixture 白名单，生产/未开关时端点伪装 404）（0b2db47，backend/src/lib/dev-auth.ts）
- 图片资产定时清理脚本 cleanup:images（0b2db47，backend/scripts/cleanup-image-assets.ts）
- 可信代理 IP 白名单限流（TRUSTED_PROXY_IPS，防止伪造转发头绕过限流）（0b2db47，backend/src/app.ts）

## 修复

- 无独立 commit 可考。代码中可见的防御性处理：PATCH 排单不复用 create 默认值避免重置字段；x-request-id 超限/含控制字符时哈希落库；COS SDK HEAD 错误路径传空 Headers 防 TypeError（0b2db47，backend/src/app.ts、lib/cos.ts）

## 重构

- 无独立 commit 可考。globals.css 中存在 `--panel`/`--line-strong` 兼容别名并标注 TODO(theme)，表明样式 token 正在向语义化迁移中途（0b2db47，app/globals.css）

## 破坏性变更

- 无（首次建档）