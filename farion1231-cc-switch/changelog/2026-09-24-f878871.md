---
repo: farion1231/cc-switch
sha: f8788719a19be6cdef39151b1c0607b4608fa10a
date: 2026-09-24
---

# 建档首版 — cc-switch @ f878871

**结论**：本档案为 farion1231/cc-switch 首次建档。项目当前处于活跃开发期（v3.20.4，HEAD 提交即建档当日），是"AI CLI 供应商切换器"这一细分类目的头部工具：单代码库同时管理 10 个 AI CLI/桌面应用的供应商、MCP、skills、prompts 配置，并内建带熔断与故障转移的本地代理。

## 建档依据

| 项 | 值 |
|---|---|
| 仓库 | https://github.com/farion1231/cc-switch |
| SHA | `f8788719a19be6cdef39151b1c0607b4608fa10a`（2026-09-24 11:58:05 +0800，`feat(icons): add Sub2API icon (#6632)`） |
| 克隆方式 | bootstrap 浅克隆 `--depth 1`（HEAD 即目标 SHA，无需补历史） |
| 分析范围 | 重点 `src-tauri/src`（Rust 后端）与 `src/`（前端）；忽略 `assets/`、图标、CHANGELOG 全文等资源文件 |

## 项目现状速览

- **定位**："All-in-One Assistant for Claude Code, Codex & Gemini CLI"（Cargo.toml:4）——桌面端配置中枢 + 本地代理。
- **体量**：Rust 后端 230 文件约 21.3 万行，前端 350 个 TS/TSX 文件；`services/provider/mod.rs`（7.6k 行）与 `services/proxy.rs`（10.8k 行）为两大复杂度热点。
- **成熟度信号**：17 张持久表的版本化 SQLite schema（含 v11→v12 加 profiles 表等多次迁移）、崩溃恢复逻辑、四语言 i18n、vitest + Rust 内嵌双测试体系、WiX/dmg/AppImage 多平台发布与签名自动更新。
- **演进方向（近期 CHANGELOG 主题）**：项目 Profiles 快照切换、代理接管与故障转移强化、用量统计与会话解析、WebDAV/S3 云同步。
- **本档案重点**：首次厘清"切换 = 物化写 live 文件"与"接管 = live 指向本地代理"这两条对立互补的主线；哨兵确认 Profile 为手动命名快照、不按目录自动应用（见 tripwires.md）。
